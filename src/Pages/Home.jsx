import React from 'react';
import { Outlet } from 'react-router-dom';
import HomePage from './HomePage';

const Home = () => {
    return (
        <div>
            <HomePage></HomePage>
        </div>
    );
};

export default Home;